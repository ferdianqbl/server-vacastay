<%- include("../../../parts/header.ejs") %>
<!-- Page Wrapper -->
<div id="wrapper">
  <%- include("../../../parts/sidebar.ejs") %>
  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">
    <!-- Main Content -->
    <div id="content">
      <%- include("../../../parts/navbar.ejs") %>

      <!-- Begin Page Content -->
      <div class="container-fluid">

        <%- include("../../../parts/message.ejs") %>

        <!-- Page Heading -->
        <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
        <!-- End Heading -->

        <ul class="nav nav-tabs" id="myTab" role="tablist">
          <li class="nav-item" role="presentation">
            <a class="nav-link active" id="home-tab" data-toggle="tab" href="#show-feature" role="tab"
              aria-controls="home" aria-selected="true">Show Feature</a>
          </li>
          <li class="nav-item" role="presentation">
            <a class="nav-link" id="profile-tab" data-toggle="tab" href="#show-activity" role="tab"
              aria-controls="profile" aria-selected="false">Show Activity</a>
          </li>
        </ul>
        <div class="tab-content" id="myTabContent">
          <%- include("./show_feature.ejs") %>
          <%- include("./show_activity.ejs") %>
        </div>

      </div>
      <!-- /.container-fluid -->
    </div>
    <!-- End of Main Content -->

    <%- include("../../../parts/footer.ejs") %>
  </div>
  <!-- End of Content Wrapper -->
</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <div class="modal-body">
        Select "Logout" below if you are ready to end your current session.
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" type="button" data-dismiss="modal">
          Cancel
        </button>
        <a class="btn btn-primary" href="login.html">Logout</a>
      </div>
    </div>
  </div>
</div>
<%- include("./edit_modal_feature.ejs") %>
<%- include("./edit_modal_activity.ejs") %>
<%- include("../../../parts/scripts.ejs") %>
<script>
  $(document).ready(function () {
    $('#dataTableActivity').DataTable();

    $("#dataTable").on("click", ".btn-update", function () {
      let id = $(this).data("id");
      $(".dataId").val(id);

      let nameFeature = $(this).data("namefeature");
      let QTY = $(this).data("qty");
      $(".dataNameFeature").val(nameFeature);
      $(".dataQTY").val(QTY);
    })

    $("#dataTableActivity").on('click', '.btn-update', function () {
      let id = $(this).data("id");
      $(".dataId").val(id);

      let nameActivity = $(this).data("nameactivity");
      let type = $(this).data("type");
      $(".dataNameActivity").val(nameActivity);
      $(".dataType").val(type);
    })
  })
  CKEDITOR.replace('description');
</script>